{
  "name": "regis-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deps": "pnpm deps-root && pnpm deps-api",
    "deps-root": "npx npm-upgrade",
    "deps-api": "cd api && npx npm-upgrade"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.1",
    "npm-upgrade": "^3.1.0",
    "typescript": "^5.3.2"
  }
}